<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto"
      xmlns:tools="http://schemas.android.com/tools"
      tools:showIn="navigation_view">

    <group android:checkableBehavior="none">
        <item
                android:id="@+id/item_category"
                android:icon="@drawable/ic_view_list"
                android:title="Categorias" />
        <item
                android:id="@+id/item_myProducts"
                android:icon="@drawable/ic_devices_other"
                android:title="Meus Produtos" />
        <item
                android:id="@+id/item_devolution"
                android:icon="@drawable/ic_pan_tool"
                android:title="Devolução" />
    </group>

    <group android:id="@+id/about" android:checkableBehavior="none">
        <item
                android:id="@+id/item_about"
                android:icon="@drawable/ic_info_outline_white"
                android:title="Sobre" />
    </group>
    <group android:id="@+id/exit" android:checkableBehavior="none">
        <item
                android:id="@+id/item_sair"
                android:icon="@drawable/ic_exit_to_app_white"
                android:title="Sair" />
    </group>
</menu>